"""Hive OS clean-install bootstrap primitives.

Bootstrap modules must remain independent of the installed Hive runtime so they
can run on an empty Termux environment before any release is trusted.
"""
