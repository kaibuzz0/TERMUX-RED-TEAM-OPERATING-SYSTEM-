# Plugin Packages

Plugin packages are deterministic `.hivepkg` ZIP bundles containing:

- `manifest.json`
- payload files
- optional `metadata.json` with signature

Packages are staged, verified, and registered DISABLED.

No auto-enable. No auto-start.
