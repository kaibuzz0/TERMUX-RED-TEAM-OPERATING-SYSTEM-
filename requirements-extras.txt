# Hive OS Optional / Legacy / AI / Networking Dependencies
# These are NOT required for core Hive OS runtime commands.
# Install only if you need legacy tools, AI/NLP features, network scanning,
# or advanced DevOps plugins.
#
# Install with: python -m pip install -r requirements-extras.txt

# Networking & Security
dnspython>=2.4.0
scapy>=2.5.0
paramiko>=3.3.0
beautifulsoup4>=4.12.0
pynacl>=1.5.0
bcrypt>=4.0.0

# Web & API
requests>=2.31.0
urllib3>=2.7.0

# AI & NLP (for brain-plug / therapist / DevAI components)
nltk>=3.8.0
numpy>=1.24.0
joblib>=1.3.0
pronouncing>=0.2.0
matplotlib>=3.7.0

# Process inspection (Windows fallback only; Linux/Termux uses /proc)
psutil>=5.9.0

# Interactive terminal experience (not used by core CLI)
colorama>=0.4.6
rich>=13.5.0
prompt-toolkit>=3.0.39
click>=8.1.0
pexpect>=4.8.0

# JSON Schema validation (not currently imported by core runtime)
jsonschema>=4.19.0
