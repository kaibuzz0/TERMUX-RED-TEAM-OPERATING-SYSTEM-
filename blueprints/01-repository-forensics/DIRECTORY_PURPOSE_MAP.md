# Directory Purpose Map

**Repository root:** `E:/Hermes-USB-Portable-main/TERMUX-RED-TEAM-OPERATING-SYSTEM-`

| Directory / File | Observed Purpose | Evidence | Classification |
|------------------|------------------|----------|----------------|
| `README.md` | Project landing, install/update/repair instructions, feature marketing | Head 200 lines | DOCUMENTATION |
| `install-termux.sh` | One-line Termux installer: checks Termux, installs packages, clones repo, symlinks `Hive Ops Final/bin/hive*`, copies boot script, prompts password+PIN base64-encoded into `~/.hive_auth/passwd` | Full read | INSTALLER |
| `install.sh` | Legacy unified installer (v1.0): installs dependencies, clones repo into `~/hive`, symlinks `Hive Ops DevAI/bin/*` | Full read | INSTALLER (legacy candidate) |
| `update.sh` | Update script: backs up `~/.hive_auth`, `~/.hive_ops.txt`, `~/.bashrc`, runs `git fetch`/`git pull`, restores credentials, re-links binaries, re-copies boot script | Full read | UPDATE |
| `emergency-repair.sh` | Repair script: preserves credentials, deletes `~/Hive-Ops`, re-clones from GitHub, re-links. `--full-nuke` also deletes credentials | Full read | REPAIR |
| `requirements.txt` | Python package dependencies with loose lower-bound pins (no upper bounds) | Full read | CONFIG |
| `.gitignore` | Ignores `mytherapist2.py`, `*.pyc`, `__pycache__/`, `.snapshots/`, `.swarm/` | Full read | CONFIG |
| `.github/workflows/ci.yml` | GitHub Actions: lint `Hive Ops DevAI`, run bandit, build a tarball excluding `brain-plug/escape_living_ai.txt` | Full read | CI/CD |
| `ENTRY` | Empty placeholder file | Full read | UNKNOWN |
| `EOF` | Empty placeholder file | Full read | UNKNOWN |
| `brain-plug/` | AI/creative-therapy module: `therapist_code only.py` (80KB Flask-based NLP/lyrics tool), `escape_living_ai.txt` (symbolic/ritual text), `README.md` | Inventory + head reads | EXPERIMENTAL / CREATIVE |
| `Hermes Plugins/` | Hermes plugin installer and plugin skeleton for Hive-Ops integration; copies files into `~/.hermes/plugins/hive-ops-plugin/` | Head reads | PLUGIN (prototype) |
| `Hive Ops DevAI/` | Python-based "DevAI" component suite: 45 `hivedev-*`/`hive-*` binaries, orchestrator/controller/agent scripts, swarm integration | Inventory + head reads | CANONICAL CANDIDATE |
| `Hive Ops Final/` | Bash + Python "Final v5.0" component suite: `bin/hive*`, `etc/env.sh`, `etc/bash-integration.sh`, Termux boot scripts, `tools/` directory with 27 security tools, `original hive os complete/` legacy subtree | Inventory + head reads | CANONICAL CANDIDATE |
| `blueprints/` | Phase 0 blueprint artifacts generated by this session | Current output | BLUEPRINT (generated) |

## Canonical Source Candidates

| Candidate | Components | Strengths | Concerns |
|-----------|------------|-----------|----------|
| `Hive Ops Final/` | 84 files, mature `hive` unified CLI, `hive-ui-v2`, `hive-secure-login`, `tools/`, boot scripts, environment integration | Has unified CLI, TUI, boot integration, documented commands | Contains nested `original hive os complete/` legacy subtree (likely historical) |
| `Hive Ops DevAI/` | 54 files, `hive-ctrl.py`, `hive-orchestrator.py`, 45 specialist `hivedev-*` binaries, swarm/core code | Explicit controller/orchestrator, larger DevAI feature set, README title matches repo marketing | Installers currently link to `Hive Ops DevAI/bin` only in `install.sh`; `install-termux.sh` links `Hive Ops Final/bin` |

**No single canonical source is yet declared.** The two main directories appear to be parallel implementations with overlapping but not identical command sets. The `README.md` describes features (`hive-ui-v2`, secure login) implemented in `Hive Ops Final/`, while the `install.sh` installs `Hive Ops DevAI/` binaries.
