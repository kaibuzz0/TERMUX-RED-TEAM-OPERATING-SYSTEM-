# Policy Engine Architecture

Single authorization authority.

- Evaluates only; never executes.
- Broker enforces.
- Deterministic, auditable decisions.
- All subsystems obtain capabilities through the engine.
