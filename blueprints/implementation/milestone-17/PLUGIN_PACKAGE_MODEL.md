# Plugin Package Model

.hivpkg deterministic ZIP containing manifest and payload.

Verification covers:

- manifest schema
- manifest digest
- signature/trust
- bundle integrity
- archive safety

Installed state defaults to DISABLED.
