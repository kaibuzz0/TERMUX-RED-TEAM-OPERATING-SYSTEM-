# Hive OS core — canonical production runtime

This directory is the canonical production runtime package for
HIVE OS FINAL PRODUCTION.

`bin/hive` at the repository root is the authoritative dispatcher.
This package contains shared runtime definitions consumed by the
installer, service loader, and release engine.
